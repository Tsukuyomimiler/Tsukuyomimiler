
      - name: 🎨 GERAR CARD METRO GOTHIC
        run: |
          # Dados da Matrix
          AVATAR_URL="${{ steps.scan-matrix.outputs.avatar_url }}"
          BIO_MATRIX="${{ steps.scan-matrix.outputs.bio_matrix }}"
          REPO_COUNT="${{ steps.scan-matrix.outputs.repo_count }}"
          FOLLOWER_COUNT="${{ steps.scan-matrix.outputs.follower_count }}"
          STAR_TOTAL="${{ steps.scan-matrix.outputs.star_total }}"
          FORK_TOTAL="${{ steps.scan-matrix.outputs.fork_total }}"
          COMMIT_COUNT="${{ steps.scan-matrix.outputs.commit_count }}"
          PR_COUNT="${{ steps.scan-matrix.outputs.pr_count }}"
          ISSUE_COUNT="${{ steps.scan-matrix.outputs.issue_count }}"
          TOTAL_XP="${{ steps.scan-matrix.outputs.total_xp }}"
          LEVEL="${{ steps.scan-matrix.outputs.level }}"
          XP_PERCENT="${{ steps.scan-matrix.outputs.xp_percent }}"
          RANK="${{ steps.scan-matrix.outputs.rank }}"
          RANK_COLOR="${{ steps.scan-matrix.outputs.rank_color }}"
          RANK_GLOW="${{ steps.scan-matrix.outputs.rank_glow }}"
          PRIMARY_COLOR="${{ steps.scan-matrix.outputs.primary_color }}"
          SECONDARY_COLOR="${{ steps.scan-matrix.outputs.secondary_color }}"
          GLOW_COLOR="${{ steps.scan-matrix.outputs.glow_color }}"
          EFFICIENCY="${{ steps.scan-matrix.outputs.efficiency }}"
          ACTIVITY_SCORE="${{ steps.scan-matrix.outputs.activity_score }}"
          IMPACT_SCORE="${{ steps.scan-matrix.outputs.impact_score }}"
          
          # Inputs do usuário
          PLAYER_NAME="${{ github.event.inputs.player_name }}"
          if [ -z "$PLAYER_NAME" ] || [ "$PLAYER_NAME" = "null" ]; then
            PLAYER_NAME="${{ github.event.inputs.github_user }}"
          fi
          
          FACTION="${{ github.event.inputs.faction }}"
          CORE_TYPE="${{ github.event.inputs.core_type }}"
          
          # Data e hora
          MATRIX_DATE=$(date +'%Y.%m.%d')
          MATRIX_TIME=$(date +'%H:%M:%S')
          UNIX_TIME=$(date +%s)
          
          # Nome do arquivo
          SAFE_NAME=$(echo "$PLAYER_NAME" | tr ' ' '_' | tr -cd '[:alnum:]_-')
          if [ -z "$SAFE_NAME" ]; then
            SAFE_NAME="metro_card"
          fi
          SVG_FILE="cards/${SAFE_NAME}_${UNIX_TIME}.svg"
          
          # Projetos top
          if [ -f top_projects.json ] && [ -s top_projects.json ]; then
            PROJECT_DATA=$(cat top_projects.json)
          else
            PROJECT_DATA='[
              {"name": "VOID-CORE", "stars": "0", "forks": "0", "lang": "BINARY"},
              {"name": "NEURAL-NET", "stars": "0", "forks": "0", "lang": "SYNTAX"},
              {"name": "QUANTUM-DB", "stars": "0", "forks": "0", "lang": "QUERY"},
              {"name": "CHAOS-ENGINE", "stars": "0", "forks": "0", "lang": "FUZZY"},
              {"name": "MATRIX-API", "stars": "0", "forks": "0", "lang": "REST"}
            ]'
          fi
          
          # Parse projetos
          PROJECT1_NAME=$(echo "$PROJECT_DATA" | jq -r '.[0].name // "VOID-CORE"' | head -c 12)
          PROJECT1_STARS=$(echo "$PROJECT_DATA" | jq -r '.[0].stars // 0')
          PROJECT1_FORKS=$(echo "$PROJECT_DATA" | jq -r '.[0].forks // 0')
          PROJECT1_LANG=$(echo "$PROJECT_DATA" | jq -r '.[0].lang // "BINARY"' | head -c 8)
          
          PROJECT2_NAME=$(echo "$PROJECT_DATA" | jq -r '.[1].name // "NEURAL-NET"' | head -c 12)
          PROJECT2_STARS=$(echo "$PROJECT_DATA" | jq -r '.[1].stars // 0')
          PROJECT2_FORKS=$(echo "$PROJECT_DATA" | jq -r '.[1].forks // 0')
          PROJECT2_LANG=$(echo "$PROJECT_DATA" | jq -r '.[1].lang // "SYNTAX"' | head -c 8)
          
          PROJECT3_NAME=$(echo "$PROJECT_DATA" | jq -r '.[2].name // "QUANTUM-DB"' | head -c 12)
          PROJECT3_STARS=$(echo "$PROJECT_DATA" | jq -r '.[2].stars // 0')
          PROJECT3_FORKS=$(echo "$PROJECT_DATA" | jq -r '.[2].forks // 0')
          PROJECT3_LANG=$(echo "$PROJECT_DATA" | jq -r '.[2].lang // "QUERY"' | head -c 8)
          
          PROJECT4_NAME=$(echo "$PROJECT_DATA" | jq -r '.[3].name // "CHAOS-ENGINE"' | head -c 12)
          PROJECT4_STARS=$(echo "$PROJECT_DATA" | jq -r '.[3].stars // 0')
          PROJECT4_FORKS=$(echo "$PROJECT_DATA" | jq -r '.[3].forks // 0')
          PROJECT4_LANG=$(echo "$PROJECT_DATA" | jq -r '.[3].lang // "FUZZY"' | head -c 8)
          
          # Calcular barras
          BAR_XP_WIDTH=$((400 * XP_PERCENT / 100))
          BAR_EFF_WIDTH=$((100 * ${EFFICIENCY%.*} / 10))
          if [ $BAR_EFF_WIDTH -gt 100 ]; then BAR_EFF_WIDTH=100; fi
          BAR_ACT_WIDTH=$((100 * ACTIVITY_SCORE / 10))
          if [ $BAR_ACT_WIDTH -gt 100 ]; then BAR_ACT_WIDTH=100; fi
          BAR_IMP_WIDTH=$((100 * IMPACT_SCORE / 1000))
          if [ $BAR_IMP_WIDTH -gt 100 ]; then BAR_IMP_WIDTH=100; fi
          
          # Calcular XP necessário e atual
          XP_NEEDED=$((1000 * (2 ** (LEVEL - 1))))
          XP_CURRENT=$((TOTAL_XP % XP_NEEDED))
          
          # Função de escape ultra segura
          matrix_escape() {
            echo "$1" | sed \
              -e 's/&/\&amp;/g' \
              -e 's/</\&lt;/g' \
              -e 's/>/\&gt;/g' \
              -e 's/"/\&quot;/g' \
              -e "s/'/\&apos;/g" \
              -e 's/\\n/ | /g' \
              -e 's/\\r//g' \
              -e 's/\\t/ /g' | tr -d '\n\r\t\0' | head -c 50
          }
          
          # Escapar tudo
          PLAYER_ESC=$(matrix_escape "$PLAYER_NAME")
          FACTION_ESC=$(matrix_escape "$FACTION")
          CORE_ESC=$(matrix_escape "$CORE_TYPE")
          BIO_ESC=$(matrix_escape "$BIO_MATRIX")
          
          P1_NAME_ESC=$(matrix_escape "$PROJECT1_NAME")
          P1_LANG_ESC=$(matrix_escape "$PROJECT1_LANG")
          P2_NAME_ESC=$(matrix_escape "$PROJECT2_NAME")
          P2_LANG_ESC=$(matrix_escape "$PROJECT2_LANG")
          P3_NAME_ESC=$(matrix_escape "$PROJECT3_NAME")
          P3_LANG_ESC=$(matrix_escape "$PROJECT3_LANG")
          P4_NAME_ESC=$(matrix_escape "$PROJECT4_NAME")
          P4_LANG_ESC=$(matrix_escape "$PROJECT4_LANG")
          
          # GERAR SVG CORRETAMENTE - MÉTODO SIMPLIFICADO
          cat > "$SVG_FILE" << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="800" height="1000" viewBox="0 0 800 1000">
<defs>
EOF
          
          # Adicionar estilos dinamicamente
          cat >> "$SVG_FILE" << EOF
<style>
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;600;700&family=Share+Tech+Mono&display=swap');
.metro-title { font-family: 'Orbitron', monospace; font-size: 36px; font-weight: 900; fill: $PRIMARY_COLOR; text-shadow: 0 0 10px $GLOW_COLOR, 0 0 20px $GLOW_COLOR; letter-spacing: 2px; }
.metro-subtitle { font-family: 'Rajdhani', sans-serif; font-size: 18px; font-weight: 600; fill: $SECONDARY_COLOR; letter-spacing: 1px; }
.metro-label { font-family: 'Share Tech Mono', monospace; font-size: 12px; fill: #AAAAAA; text-transform: uppercase; letter-spacing: 1px; }
.metro-value { font-family: 'Orbitron', monospace; font-size: 20px; font-weight: 700; fill: white; }
.metro-rank { font-family: 'Orbitron', monospace; font-size: 64px; font-weight: 900; fill: $RANK_COLOR; text-shadow: 0 0 15px $RANK_GLOW, 0 0 30px $RANK_GLOW; }
.metro-xp { font-family: 'Share Tech Mono', monospace; font-size: 14px; fill: #CCCCCC; }
.metro-project { font-family: 'Rajdhani', sans-serif; font-size: 11px; fill: #DDDDDD; }
.metro-bio { font-family: 'Share Tech Mono', monospace; font-size: 10px; fill: #BBBBBB; line-height: 1.3; }
</style>
EOF
          
          cat >> "$SVG_FILE" << EOF
<linearGradient id="metroBg" x1="0%" y1="0%" x2="100%" y2="100%">
<stop offset="0%" stop-color="#0A0A0A"/>
<stop offset="50%" stop-color="#111111"/>
<stop offset="100%" stop-color="#0A0A0A"/>
</linearGradient>
<linearGradient id="primaryGrad" x1="0%" y1="0%" x2="100%" y2="0%">
<stop offset="0%" stop-color="$PRIMARY_COLOR" stop-opacity="0.7"/>
<stop offset="50%" stop-color="$SECONDARY_COLOR" stop-opacity="0.9"/>
<stop offset="100%" stop-color="$PRIMARY_COLOR" stop-opacity="0.7"/>
</linearGradient>
<linearGradient id="xpBarGrad" x1="0%" y1="0%" x2="100%" y2="0%">
<stop offset="0%" stop-color="$PRIMARY_COLOR"/>
<stop offset="50%" stop-color="$GLOW_COLOR"/>
<stop offset="100%" stop-color="$SECONDARY_COLOR"/>
</linearGradient>
<pattern id="gridPattern" width="40" height="40" patternUnits="userSpaceOnUse">
<path d="M 40 0 L 0 0 0 40" fill="none" stroke="$PRIMARY_COLOR" stroke-width="0.5" opacity="0.3"/>
</pattern>
<pattern id="circuitPattern" width="100" height="100" patternUnits="userSpaceOnUse">
<circle cx="50" cy="50" r="2" fill="$SECONDARY_COLOR" opacity="0.2"/>
<path d="M20,50 H80 M50,20 V80" stroke="$PRIMARY_COLOR" stroke-width="0.3" opacity="0.1" fill="none"/>
</pattern>
<filter id="metroGlow">
<feGaussianBlur stdDeviation="3" result="blur"/>
<feMerge>
<feMergeNode in="blur"/>
<feMergeNode in="SourceGraphic"/>
</feMerge>
</filter>
<filter id="neonGlow">
<feGaussianBlur stdDeviation="5" result="blur"/>
<feFlood flood-color="$GLOW_COLOR" flood-opacity="0.5"/>
<feComposite in2="blur" operator="in"/>
<feMerge>
<feMergeNode in="blur"/>
<feMergeNode in="SourceGraphic"/>
</feMerge>
</filter>
<clipPath id="barClip">
<rect x="0" y="0" width="400" height="20" rx="10"/>
</clipPath>
</defs>
EOF
          
          # Estrutura principal do SVG
          cat >> "$SVG_FILE" << EOF
<!-- FUNDO METRO -->
<rect width="800" height="1000" fill="url(#metroBg)"/>
<rect width="800" height="1000" fill="url(#gridPattern)" opacity="0.1"/>
<rect width="800" height="1000" fill="url(#circuitPattern)" opacity="0.05"/>

<!-- ELEMENTOS DE FUNDO DINÂMICOS -->
<circle cx="100" cy="150" r="60" fill="$PRIMARY_COLOR" opacity="0.05" filter="url(#metroGlow)"/>
<circle cx="700" cy="800" r="80" fill="$SECONDARY_COLOR" opacity="0.05" filter="url(#metroGlow)"/>
<rect x="300" y="400" width="200" height="200" rx="20" transform="rotate(45 400 500)" fill="$GLOW_COLOR" opacity="0.03"/>

<!-- CARD PRINCIPAL -->
<g filter="url(#neonGlow)">
<rect x="50" y="50" width="700" height="900" rx="30" fill="#111111" opacity="0.95" stroke="$PRIMARY_COLOR" stroke-width="3"/>
</g>

<!-- CABEÇALHO METRO -->
<rect x="50" y="50" width="700" height="120" rx="30" fill="url(#primaryGrad)" opacity="0.8"/>
<rect x="50" y="50" width="700" height="120" rx="30" fill="none" stroke="$GLOW_COLOR" stroke-width="2" opacity="0.6"/>

<!-- NOME DO JOGADOR -->
<text x="400" y="110" text-anchor="middle" class="metro-title">$PLAYER_ESC</text>

<!-- FACÇÃO E NÚCLEO -->
<text x="400" y="140" text-anchor="middle" class="metro-subtitle">$FACTION_ESC • $CORE_ESC CORE</text>

<!-- SEÇÃO DE STATUS PRINCIPAL -->
<g transform="translate(100, 200)">
<!-- NÍVEL -->
<rect x="0" y="0" width="180" height="80" rx="15" fill="#1A1A1A" opacity="0.9"/>
<rect x="0" y="0" width="180" height="80" rx="15" fill="none" stroke="$PRIMARY_COLOR" stroke-width="2" opacity="0.5"/>
<text x="90" y="30" text-anchor="middle" class="metro-label">NÍVEL</text>
<text x="90" y="60" text-anchor="middle" class="metro-value">$LEVEL</text>

<!-- RANK -->
<rect x="200" y="0" width="180" height="80" rx="15" fill="#1A1A1A" opacity="0.9"/>
<rect x="200" y="0" width="180" height="80" rx="15" fill="none" stroke="$RANK_COLOR" stroke-width="2" opacity="0.7"/>
<text x="290" y="30" text-anchor="middle" class="metro-label">RANK</text>
<text x="290" y="70" text-anchor="middle" class="metro-rank">$RANK</text>

<!-- XP TOTAL -->
<rect x="400" y="0" width="180" height="80" rx="15" fill="#1A1A1A" opacity="0.9"/>
<rect x="400" y="0" width="180" height="80" rx="15" fill="none" stroke="$SECONDARY_COLOR" stroke-width="2" opacity="0.5"/>
<text x="490" y="30" text-anchor="middle" class="metro-label">XP TOTAL</text>
<text x="490" y="60" text-anchor="middle" class="metro-value">$TOTAL_XP</text>
</g>

<!-- BARRA DE XP MEGA -->
<g transform="translate(100, 300)">
<text x="0" y="0" class="metro-label">EXPERIÊNCIA MATRIX</text>
<text x="400" y="0" class="metro-xp" text-anchor="end">${XP_PERCENT}% • ${XP_CURRENT}/${XP_NEEDED} XP</text>

<!-- BARRA DE FUNDO -->
<rect x="0" y="20" width="400" height="30" rx="15" fill="#0A0A0A"/>
<rect x="0" y="20" width="400" height="30" rx="15" fill="none" stroke="#333333" stroke-width="2"/>

<!-- BARRA DE XP ANIMADA -->
<rect x="0" y="20" width="${BAR_XP_WIDTH}" height="30" rx="15" fill="url(#xpBarGrad)" clip-path="url(#barClip)"/>

<!-- MARCADORES DE NÍVEL -->
<circle cx="80" cy="35" r="3" fill="white" opacity="0.5"/>
<circle cx="160" cy="35" r="3" fill="white" opacity="0.5"/>
<circle cx="240" cy="35" r="3" fill="white" opacity="0.5"/>
<circle cx="320" cy="35" r="3" fill="white" opacity="0.5"/>

<!-- EFEITO DE PULSO NA BARRA -->
<rect x="0" y="20" width="${BAR_XP_WIDTH}" height="30" rx="15" fill="none" stroke="$GLOW_COLOR" stroke-width="1" opacity="0.7">
<animate attributeName="opacity" values="0.3;0.7;0.3" dur="2s" repeatCount="indefinite"/>
</rect>
</g>

<!-- BIO MATRIX -->
<g transform="translate(100, 360)">
<rect x="0" y="0" width="600" height="60" rx="10" fill="#1A1A1A" opacity="0.8"/>
<text x="10" y="20" class="metro-label">📡 MATRIX BIO</text>
<text x="10" y="40" class="metro-bio">$BIO_ESC</text>
</g>

<!-- STATS GITHUB EM GRADE 3x2 -->
<g transform="translate(100, 440)">
<text x="0" y="0" class="metro-label">STATS GITHUB</text>

<g transform="translate(0, 30)">
<!-- LINHA 1 -->
<!-- REPOS -->
<rect x="0" y="0" width="190" height="70" rx="10" fill="#222222" opacity="0.9"/>
<text x="95" y="25" text-anchor="middle" class="metro-label">Repositórios</text>
<text x="95" y="50" text-anchor="middle" class="metro-value" fill="$PRIMARY_COLOR">$REPO_COUNT</text>

<!-- FOLLOWERS -->
<rect x="210" y="0" width="190" height="70" rx="10" fill="#222222" opacity="0.9"/>
<text x="305" y="25" text-anchor="middle" class="metro-label">Seguidores</text>
<text x="305" y="50" text-anchor="middle" class="metro-value" fill="$SECONDARY_COLOR">$FOLLOWER_COUNT</text>

<!-- STARS -->
<rect x="420" y="0" width="190" height="70" rx="10" fill="#222222" opacity="0.9"/>
<text x="515" y="25" text-anchor="middle" class="metro-label">Estrelas</text>
<text x="515" y="50" text-anchor="middle" class="metro-value" fill="$GLOW_COLOR">$STAR_TOTAL</text>

<!-- LINHA 2 -->
<!-- COMMITS -->
<rect x="0" y="90" width="190" height="70" rx="10" fill="#222222" opacity="0.9"/>
<text x="95" y="115" text-anchor="middle" class="metro-label">Commits</text>
<text x="95" y="140" text-anchor="middle" class="metro-value">$COMMIT_COUNT</text>

<!-- PRs -->
<rect x="210" y="90" width="190" height="70" rx="10" fill="#222222" opacity="0.9"/>
<text x="305" y="115" text-anchor="middle" class="metro-label">Pull Requests</text>
<text x="305" y="140" text-anchor="middle" class="metro-value">$PR_COUNT</text>

<!-- ISSUES -->
<rect x="420" y="90" width="190" height="70" rx="10" fill="#222222" opacity="0.9"/>
<text x="515" y="115" text-anchor="middle" class="metro-label">Issues</text>
<text x="515" y="140" text-anchor="middle" class="metro-value">$ISSUE_COUNT</text>
</g>
</g>

<!-- BARRAS DE MÉTRICAS AVANÇADAS -->
<g transform="translate(100, 620)">
<text x="0" y="0" class="metro-label">MÉTRICAS AVANÇADAS</text>

<g transform="translate(0, 30)">
<!-- EFICIÊNCIA -->
<text x="0" y="15" class="metro-xp">EFICIÊNCIA</text>
<rect x="100" y="5" width="100" height="15" rx="7" fill="#1A1A1A"/>
<rect x="100" y="5" width="$BAR_EFF_WIDTH" height="15" rx="7" fill="$PRIMARY_COLOR"/>
<text x="205" y="15" class="metro-xp">$EFFICIENCY</text>

<!-- ATIVIDADE -->
<text x="0" y="45" class="metro-xp">ATIVIDADE</text>
<rect x="100" y="35" width="100" height="15" rx="7" fill="#1A1A1A"/>
<rect x="100" y="35" width="$BAR_ACT_WIDTH" height="15" rx="7" fill="$SECONDARY_COLOR"/>
<text x="205" y="45" class="metro-xp">${ACTIVITY_SCORE}/dia</text>

<!-- IMPACTO -->
<text x="0" y="75" class="metro-xp">IMPACTO</text>
<rect x="100" y="65" width="100" height="15" rx="7" fill="#1A1A1A"/>
<rect x="100" y="65" width="$BAR_IMP_WIDTH" height="15" rx="7" fill="$GLOW_COLOR"/>
<text x="205" y="75" class="metro-xp">$IMPACT_SCORE</text>
</g>
</g>

<!-- PROJETOS TOP EM GRADE -->
<g transform="translate(100, 730)">
<text x="0" y="0" class="metro-label">PROJETOS MATRIX</text>

<g transform="translate(0, 30)">
<!-- PROJETO 1 -->
<rect x="0" y="0" width="150" height="80" rx="10" fill="#222222" opacity="0.9"/>
<text x="75" y="20" text-anchor="middle" class="metro-project" font-weight="600">$P1_NAME_ESC</text>
<text x="75" y="35" text-anchor="middle" class="metro-project">⭐ $PROJECT1_STARS 🍴 $PROJECT1_FORKS</text>
<text x="75" y="50" text-anchor="middle" class="metro-project">⚙️ $P1_LANG_ESC</text>

<!-- PROJETO 2 -->
<rect x="160" y="0" width="150" height="80" rx="10" fill="#222222" opacity="0.9"/>
<text x="235" y="20" text-anchor="middle" class="metro-project" font-weight="600">$P2_NAME_ESC</text>
<text x="235" y="35" text-anchor="middle" class="metro-project">⭐ $PROJECT2_STARS 🍴 $PROJECT2_FORKS</text>
<text x="235" y="50" text-anchor="middle" class="metro-project">⚙️ $P2_LANG_ESC</text>

<!-- PROJETO 3 -->
<rect x="320" y="0" width="150" height="80" rx="10" fill="#222222" opacity="0.9"/>
<text x="395" y="20" text-anchor="middle" class="metro-project" font-weight="600">$P3_NAME_ESC</text>
<text x="395" y="35" text-anchor="middle" class="metro-project">⭐ $PROJECT3_STARS 🍴 $PROJECT3_FORKS</text>
<text x="395" y="50" text-anchor="middle" class="metro-project">⚙️ $P3_LANG_ESC</text>

<!-- PROJETO 4 -->
<rect x="480" y="0" width="150" height="80" rx="10" fill="#222222" opacity="0.9"/>
<text x="555" y="20" text-anchor="middle" class="metro-project" font-weight="600">$P4_NAME_ESC</text>
<text x="555" y="35" text-anchor="middle" class="metro-project">⭐ $PROJECT4_STARS 🍴 $PROJECT4_FORKS</text>
<text x="555" y="50" text-anchor="middle" class="metro-project">⚙️ $P4_LANG_ESC</text>
</g>
</g>

<!-- RODAPÉ METRO -->
<g transform="translate(100, 850)">
<rect x="0" y="0" width="600" height="40" rx="10" fill="#0A0A0A" opacity="0.9"/>
<text x="10" y="25" class="metro-xp">⚡ METRO GOTHIC XP SYSTEM • $MATRIX_DATE $MATRIX_TIME</text>
<text x="590" y="25" class="metro-xp" text-anchor="end">ID: $UNIX_TIME</text>
</g>

<!-- ANIMAÇÕES E EFEITOS -->
<!-- PONTOS BRILHANTES -->
<circle cx="100" cy="100" r="2" fill="$GLOW_COLOR" opacity="0.8">
<animate attributeName="opacity" values="0.3;1;0.3" dur="1.5s" repeatCount="indefinite"/>
</circle>
<circle cx="700" cy="200" r="2" fill="$PRIMARY_COLOR" opacity="0.8">
<animate attributeName="opacity" values="0.3;1;0.3" dur="2s" repeatCount="indefinite"/>
</circle>
<circle cx="150" cy="900" r="2" fill="$SECONDARY_COLOR" opacity="0.8">
<animate attributeName="opacity" values="0.3;1;0.3" dur="2.5s" repeatCount="indefinite"/>
</circle>

<!-- LINHAS SCAN -->
<line x1="50" y1="180" x2="750" y2="180" stroke="$GLOW_COLOR" stroke-width="1" opacity="0.3">
<animate attributeName="opacity" values="0.1;0.5;0.1" dur="3s" repeatCount="indefinite"/>
</line>

<!-- CÓDIGO BINÁRIO DECORATIVO -->
<text x="750" y="50" text-anchor="end" class="metro-xp" opacity="0.1">10101010</text>
<text x="750" y="70" text-anchor="end" class="metro-xp" opacity="0.1">01010101</text>
<text x="50" y="980" class="metro-xp" opacity="0.1">SYSTEM: ONLINE</text>
</svg>
EOF
          
          echo "⚡✅ CARD METRO GOTHIC CRIADO: $SVG_FILE"
          echo "🎮 Sistema XP: Nível $LEVEL | Rank $RANK"
          echo "📊 XP Total: $TOTAL_XP ($XP_PERCENT% progresso)"
          echo "🔥 Métricas: Eficiência $EFFICIENCY | Atividade $ACTIVITY_SCORE/dia"
          echo "💫 Cores: $PRIMARY_COLOR | $SECONDARY_COLOR | $GLOW_COLOR"
          echo "✅ Card gerado com SUCESSO ABSOLUTO!"
