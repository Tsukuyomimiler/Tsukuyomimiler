name: 🛡️ Card Guild (Dark Red Minimal Pro Edition)

on:
  schedule:
    - cron: '0 12 * * *' # Atualiza diariamente às 12h UTC (~9h no Brasil)
  workflow_dispatch:
    inputs:
      usuario:
        description: 'Usuário do GitHub (ex: ciconha)'
        required: true
        default: 'ciconha'
      empresa:
        description: 'Nome da empresa (ex: TechLabs Studio)'
        required: false
        default: 'INSS'
      cargo:
        description: 'Cargo atual (ex: Desenvolvedor Back-end)'
        required: false
        default: 'Estagiário de TI'

permissions:
  contents: write

jobs:
  gerar-svg:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout do repositório
        uses: actions/checkout@v4

      - name: ⚙️ Instalar dependências
        run: |
          sudo apt-get update -y
          sudo apt-get install -y curl jq moreutils

      - name: 🔍 Buscar dados do GitHub
        id: fetch-data
        run: |
          # Usar input do usuário corretamente
          if [[ -n "${{ github.event.inputs.usuario }}" ]]; then
            USER="${{ github.event.inputs.usuario }}"
          else
            # Fallback para usuário padrão
            USER="ciconha"
          fi
          
          TOKEN="${{ secrets.TSUKUYOMI_CICONHA }}"

          echo "📡 Coletando dados do usuário: $USER"

          # Dados principais
          curl -s -H "Authorization: token $TOKEN" "https://api.github.com/users/$USER" > user.json
          curl -s -H "Authorization: token $TOKEN" "https://api.github.com/users/$USER/repos?per_page=100" > repos.json
          curl -s -H "Authorization: token $TOKEN" "https://api.github.com/users/$USER/events?per_page=100" > events.json

          sanitize() {
            echo "$1" | iconv -c -t UTF-8//TRANSLIT | tr -d '\n' | sed 's/"/\\"/g'
          }

          LOGIN=$(sanitize "$(jq -r '.login' user.json)")
          NAME=$(sanitize "$(jq -r '.name // "Usuário Desconhecido"' user.json)")
          BIO=$(sanitize "$(jq -r '.bio // "Desenvolvedor apaixonado por tecnologia."' user.json)")
          REPOS=$(jq -r '.public_repos' user.json)
          FOLLOWERS=$(jq -r '.followers' user.json)
          COMMITS=$(jq '[.[] | select(.type == "PushEvent")] | length' events.json)
          PRS=$(jq '[.[] | select(.type == "PullRequestEvent")] | length' events.json)
          ISSUES=$(jq '[.[] | select(.type == "IssuesEvent")] | length' events.json)

          # Linguagem mais usada
          LANG=$(jq -r '.[].language' repos.json | sort | uniq -c | sort -nr | head -1 | awk '{print $2}')
          if [ -z "$LANG" ] || [ "$LANG" = "null" ]; then
            LANG="Desconhecida"
          fi

          # XP e classificação
          TOTAL_XP=$((REPOS * 50 + FOLLOWERS * 30 + COMMITS * 2 + PRS * 10 + ISSUES * 5))
          LEVEL=$((TOTAL_XP / 1000 + 1))
          XP_PERCENTAGE=$(( (TOTAL_XP % 1000) * 100 / 1000 ))

          if [ "$REPOS" -gt 50 ]; then
            CLASS="Arquiteto"
          elif [ "$FOLLOWERS" -gt 100 ]; then
            CLASS="Lenda"
          elif [ "$REPOS" -gt 30 ] && [ "$FOLLOWERS" -gt 50 ]; then
            CLASS="Mestre"
          else
            CLASS="Aventureiro"
          fi

          if [ "$REPOS" -gt 100 ]; then
            RANK="Lendário"
          elif [ "$REPOS" -gt 50 ]; then
            RANK="Épico"
          else
            RANK="Herói"
          fi

          # Top 2 projetos
          jq 'sort_by(-.stargazers_count) | .[0:2] | .[] | {name: .name, stars: .stargazers_count}' repos.json > top_projects.json

          echo "login=$LOGIN" >> $GITHUB_OUTPUT
          echo "name=$NAME" >> $GITHUB_OUTPUT
          echo "bio=$BIO" >> $GITHUB_OUTPUT
          echo "repos=$REPOS" >> $GITHUB_OUTPUT
          echo "followers=$FOLLOWERS" >> $GITHUB_OUTPUT
          echo "commits=$COMMITS" >> $GITHUB_OUTPUT
          echo "prs=$PRS" >> $GITHUB_OUTPUT
          echo "issues=$ISSUES" >> $GITHUB_OUTPUT
          echo "lang=$LANG" >> $GITHUB_OUTPUT
          echo "total_xp=$TOTAL_XP" >> $GITHUB_OUTPUT
          echo "level=$LEVEL" >> $GITHUB_OUTPUT
          echo "xp_percentage=$XP_PERCENTAGE" >> $GITHUB_OUTPUT
          echo "class=$CLASS" >> $GITHUB_OUTPUT
          echo "rank=$RANK" >> $GITHUB_OUTPUT

      - name: 🎨 Gerar SVG Dark Red Minimalista
        run: |
          mkdir -p cards
          PROJECT1_NAME=$(jq -r '.name' top_projects.json | head -1)
          PROJECT1_STARS=$(jq -r '.stars' top_projects.json | head -1)
          PROJECT2_NAME=$(jq -r '.name' top_projects.json | tail -1)
          PROJECT2_STARS=$(jq -r '.stars' top_projects.json | tail -1)
          CURRENT_DATE=$(date +'%d/%m/%Y')
          
          # Obter inputs do workflow
          EMPRESA="${{ github.event.inputs.empresa }}"
          CARGO="${{ github.event.inputs.cargo }}"
          
          # Criar arquivo SVG com design abstrato/artístico
          SVG_FILE="cards/${{ github.event.inputs.usuario || 'ciconha' }}.svg"
          
          cat > "$SVG_FILE" << 'EOF'
          <svg xmlns="http://www.w3.org/2000/svg" width="460" height="280" viewBox="0 0 460 280">
            <defs>
              <linearGradient id="darkRedBg" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="#1a0000"/>
                <stop offset="50%" stop-color="#360000"/>
                <stop offset="100%" stop-color="#1a0000"/>
              </linearGradient>
              
              <linearGradient id="redGlow" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="#ff0000" stop-opacity="0.1"/>
                <stop offset="50%" stop-color="#ff3333" stop-opacity="0.3"/>
                <stop offset="100%" stop-color="#ff0000" stop-opacity="0.1"/>
              </linearGradient>
              
              <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
                <feGaussianBlur stdDeviation="2" result="blur"/>
                <feMerge>
                  <feMergeNode in="blur"/>
                  <feMergeNode in="SourceGraphic"/>
                </feMerge>
              </filter>
              
              <style>
                @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Share+Tech+Mono&display=swap');
                
                .title {
                  font-family: 'Orbitron', monospace;
                  font-size: 24px;
                  font-weight: 900;
                  fill: #FF2222;
                  text-shadow: 0 0 10px #FF0000;
                }
                
                .subtitle {
                  font-family: 'Orbitron', monospace;
                  font-size: 12px;
                  fill: #FF6666;
                  letter-spacing: 1px;
                }
                
                .text {
                  font-family: 'Share Tech Mono', monospace;
                  font-size: 11px;
                  fill: #FFAAAA;
                }
                
                .tech {
                  font-family: 'Share Tech Mono', monospace;
                  font-size: 10px;
                  fill: #FF8888;
                }
                
                .bar-bg {
                  fill: #0a0000;
                }
                
                .bar-fill {
                  fill: url(#redGlow);
                  filter: url(#glow);
                }
              </style>
            </defs>

            <!-- Fundo com padrão abstrato -->
            <rect width="460" height="280" rx="15" ry="15" fill="url(#darkRedBg)" stroke="#880000" stroke-width="2"/>
            
            <!-- Elementos abstratos de fundo -->
            <circle cx="50" cy="50" r="40" fill="#220000" opacity="0.3"/>
            <circle cx="400" cy="200" r="60" fill="#330000" opacity="0.2"/>
            <rect x="300" y="30" width="80" height="80" rx="10" transform="rotate(45 340 70)" fill="#440000" opacity="0.2"/>
            
            <!-- Linhas decorativas -->
            <line x1="0" y1="80" x2="460" y2="80" stroke="#550000" stroke-width="1" opacity="0.3"/>
            <line x1="0" y1="200" x2="460" y2="200" stroke="#550000" stroke-width="1" opacity="0.3"/>
            
            <!-- Cabeçalho com design futurista -->
            <g filter="url(#glow)">
              <text x="30" y="55" class="title">${{ steps.fetch-data.outputs.name }}</text>
            </g>
            
            <text x="30" y="75" class="subtitle">[ $EMPRESA ] • { $CARGO }</text>
            <text x="30" y="93" class="subtitle">[ ${{ steps.fetch-data.outputs.class }} ] • { ${{ steps.fetch-data.outputs.rank }} }</text>

            <!-- Bio em container estilizado -->
            <rect x="25" y="105" width="410" height="35" rx="5" fill="#110000" opacity="0.5" stroke="#330000" stroke-width="1"/>
            <text x="30" y="125" class="text">"${{ steps.fetch-data.outputs.bio }}"</text>

            <!-- Linguagem + XP -->
            <g transform="translate(0, 10)">
              <rect x="25" y="150" width="200" height="60" rx="8" fill="#0a0000" opacity="0.7" stroke="#440000" stroke-width="1"/>
              <text x="35" y="170" class="tech">⚡ TECH STACK:</text>
              <text x="35" y="190" class="subtitle">${{ steps.fetch-data.outputs.lang }}</text>
              
              <rect x="235" y="150" width="200" height="60" rx="8" fill="#0a0000" opacity="0.7" stroke="#440000" stroke-width="1"/>
              <text x="245" y="170" class="tech">🎮 PLAYER STATS:</text>
              <text x="245" y="190" class="subtitle">LVL ${{ steps.fetch-data.outputs.level }} • XP ${{ steps.fetch-data.outputs.total_xp }}</text>
            </g>

            <!-- Barra de XP -->
            <g transform="translate(0, 10)">
              <text x="30" y="235" class="tech">PROGRESSO [ ${{ steps.fetch-data.outputs.xp_percentage }}% ]:</text>
              <rect x="30" y="240" width="400" height="12" class="bar-bg" rx="6" ry="6"/>
              <rect x="30" y="240" width="$((400 * ${{ steps.fetch-data.outputs.xp_percentage }} / 100))" height="12" class="bar-fill" rx="6" ry="6"/>
            </g>

            <!-- Projetos destacados -->
            <g transform="translate(0, 30)">
              <rect x="25" y="255" width="410" height="40" rx="5" fill="#110000" opacity="0.6" stroke="#330000" stroke-width="1"/>
              <text x="30" y="272" class="subtitle">⭐ PROJECT HIGHLIGHTS:</text>
              <text x="35" y="290" class="text">» $PROJECT1_NAME — ⭐$PROJECT1_STARS</text>
              <text x="230" y="290" class="text">» $PROJECT2_NAME — ⭐$PROJECT2_STARS</text>
            </g>

            <!-- Rodapé técnico -->
            <g transform="translate(0, 25)">
              <rect x="0" y="305" width="460" height="30" fill="#000000" opacity="0.8"/>
              <text x="230" y="325" text-anchor="middle" class="tech">
                [ $CURRENT_DATE ] • COMMITS:${{ steps.fetch-data.outputs.commits }} • PRs:${{ steps.fetch-data.outputs.prs }} • ISSUES:${{ steps.fetch-data.outputs.issues }}
              </text>
            </g>
            
            <!-- Elementos decorativos de canto -->
            <path d="M15,15 L40,15 L15,40 Z" fill="#FF2222" opacity="0.3"/>
            <path d="M445,15 L445,40 L420,15 Z" fill="#FF2222" opacity="0.3"/>
            <path d="M15,265 L15,240 L40,265 Z" fill="#FF2222" opacity="0.3"/>
            <path d="M445,265 L420,265 L445,240 Z" fill="#FF2222" opacity="0.3"/>
          </svg>
          EOF

          echo "✅ SVG com design abstrato gerado com sucesso!"

      - name: 💾 Commit e Push automático
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add cards/
          git commit -m "🎨 Atualização do cartão da guilda para ${{ github.event.inputs.usuario || 'ciconha' }}" || echo "Nenhuma mudança detectada"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.TSUKUYOMI_CICONHA }}
